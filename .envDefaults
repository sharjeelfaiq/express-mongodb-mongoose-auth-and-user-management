# MongoDB Connection URI
# The connection string to connect to MongoDB. Make sure to store this securely and do not expose it in public repositories.
MONGODB_URI=""

# Application Port
# The port on which the application will run. Default is 5000 if not specified.
PORT=5000

# JWT Secret Key
# A secret key used to sign and verify JWT tokens. This should be kept private and secure.
# Ensure that this value is complex and not easily guessable.
JWT_SECRET=""

# JWT Expiration Time : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# The time (in seconds) for which the JWT token is valid. For example, "3600" means the token will expire in 1 hour.
JWT_EXPIRATION=""
